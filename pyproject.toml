[project]
name = "smbuilder"
version = "0.1.0"
description = "Generate c-file with statemachine defined in *.yaml"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
]
